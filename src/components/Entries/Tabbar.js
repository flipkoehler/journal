export default function Tabbar() {
  return (
    //The tab bar has two tabs, each with a badge with the number of entries.

    <ul>
      <li>All Entries</li>
      <li>Favorites</li>
    </ul>
  );
}
